function on_resize(c, t) {
    return onresize = function() {
        clearTimeout(t), t = setTimeout(c, 100);
    }, c;
}

!function($) {
    $(document).ready(function() {});
}(jQuery), $(document).ready(function() {
    function resetBtn() {
        var menuBtnComponent1 = $(".menu-top"), menuBtnComponent2 = $(".menu-middle"), menuBtnComponent3 = $(".menu-bottom");
        $("body").removeClass("active-sidebar"), menuBtnComponent1.removeClass("menu-top-click"), 
        menuBtnComponent2.removeClass("menu-middle-click"), menuBtnComponent3.removeClass("menu-bottom-click");
    }
    function resetManus(deskTopMq) {
        deskTopMq.matches, resetBtn();
    }
    var breakpoint = {};
    breakpoint.refreshValue = function() {
        this.value = window.getComputedStyle(document.querySelector("body"), ":before").getPropertyValue("content").replace(/'/g, "");
    }, $(window).resize(function() {
        breakpoint.refreshValue();
    }).resize();
    var panels = $(".panel"), addClassToPanels = function() {
        "tablet-portrait" === breakpoint.value || "tablet" === breakpoint.value ? $(panels).addClass("tablet-layout") : $(panels).removeClass("tablet-layout");
    };
    addClassToPanels(), on_resize(function() {
        addClassToPanels();
    });
    var showSidebar = function() {
        $("body").removeClass("active-nav").toggleClass("active-sidebar"), $(".menu-button").removeClass("active-button");
    };
    $(".menu-button-mb").on("click", function() {
        var $this = $(this);
        $this.find(".menu-top").toggleClass("menu-top-click"), $this.find(".menu-middle").toggleClass("menu-middle-click"), 
        $this.find(".menu-bottom").toggleClass("menu-bottom-click"), showSidebar();
    });
    var deskTopMq = window.matchMedia("(min-width: 736px)");
    deskTopMq.addListener(resetManus), resetManus(deskTopMq);
});